{
  "name": "fit-app",
  "version": "1.0.0",
  "description": "Live Workout Timer App with NestJS and React",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "install-all": "pnpm install",
    "dev": "concurrently \"pnpm -C server dev\" \"pnpm -C client dev\"",
    "start": "pnpm -C server build && pnpm -C client build && pnpm -C server start",
    "build": "pnpm -C server build && pnpm -C client build"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
